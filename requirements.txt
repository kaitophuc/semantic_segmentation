# Project Python dependencies that are safe to install after the Jetson-specific
# PyTorch/TensorRT setup has been completed.
#
# Do not pin generic PyPI PyTorch CUDA wheels here. Jetson Orin/Nano devices
# need NVIDIA Jetson-compatible PyTorch builds, installed by
# setup_jetson_nano_orin.sh.
transformers
accelerate
opencv-python
numpy
scipy>=1.15
safetensors
pillow
tqdm
onnx

# Required by current Jetson PyTorch wheels. The setup script also adds its
# library folder to LD_LIBRARY_PATH when the venv is activated.
nvidia-cudss-cu12
